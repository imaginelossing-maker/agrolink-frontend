import {useNavigate} from "react-router-dom"; export default function Login(){ const navigate=useNavigate(); const handleLogin=(role)=>{ localStorage.setItem("role",role); if(role==='seller') navigate('/seller-dashboard'); if(role==='buyer') navigate('/buyer-dashboard'); if(role==='admin') navigate('/admin-dashboard'); }; return (<div><h2>Select Login Role</h2><button onClick={()=>handleLogin('seller')}>Seller Login</button><button onClick={()=>handleLogin('buyer')}>Buyer Login</button><button onClick={()=>handleLogin('admin')}>Admin Login</button></div>)}