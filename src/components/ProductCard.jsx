export default function ProductCard({product}){ const addToCart=(product)=>{const cart=JSON.parse(localStorage.getItem("cart"))||[]; cart.push(product); localStorage.setItem("cart",JSON.stringify(cart)); alert("Added to Cart")}; const contactFarmer=(product)=>window.open(`https://wa.me/${product.phone}`); return(<div><img src={product.image} alt={product.name}/><h3>{product.name}</h3><p>₦{product.price}</p><button onClick={()=>addToCart(product)}>Add To Cart</button><button onClick={()=>contactFarmer(product)}>Contact Farmer</button></div>) }